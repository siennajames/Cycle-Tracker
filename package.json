{
  "name": "cycle-tracker",
  "version": "1.0.0",
  "description": "Static site for CycleSync — a client-side menstrual cycle tracker",
  "scripts": {
    "start": "http-server . -p 3000 --proxy http://localhost:3000?"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
